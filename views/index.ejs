<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Student Result Management System</title>
    
    <link rel="stylesheet" href="../public/style.css">
    <link rel="stylesheet" href="/style.css">

</head>

<body>
    <div class="container">
        <img src="/images/logo.png" alt="Logo" class="logo">
        <h1>Welcome to SRMS</h1>
        <p>Manage your academic results with ease</p>
        <a href="./login.ejs" class="btn">Login</a>
        <a href="./register.ejs" class="btn">Register</a>
    </div>
</body>

</html> -->

<!DOCTYPE html>
<html>

<head>
    <title>Dashboard</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <div class="container">
        <h2>Welcome, <%= user.studentId %>!</h2>

        <% if (result) { %>
            <div class="result-card">
                <h3>Your Result</h3>
                <ul>
                    <% result.subjects.forEach(subject=> { %>
                        <li>
                            <%= subject.name %>: <%= subject.marks %>
                        </li>
                        <% }) %>
                </ul>
                <p><strong>Total Marks:</strong>
                    <%= result.totalMarks %>
                </p>
                <p><strong>Percentage:</strong>
                    <%= result.percentage.toFixed(2) %> %
                </p>
                <p><strong>Grade:</strong>
                    <%= result.grade %>
                </p>
            </div>
            <% } else { %>
                <p>No result found for your ID.</p>
                <% } %>

                    <a href="/logout"><button>Logout</button></a>
    </div>
</body>

</html>